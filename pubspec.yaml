name: miniproject
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  qr_flutter: ^4.1.0
  encrypt: ^5.0.1

  # ✅ Updated Firebase (modern versions)
  firebase_core: ^2.30.0
  firebase_auth: ^4.19.0
  cloud_firestore: ^4.17.5
  firebase_database: ^10.5.7

  # ⚠️ qrscan is outdated → replace with mobile_scanner
  mobile_scanner: ^3.5.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/Teacher.png
    - assets/Student.png
